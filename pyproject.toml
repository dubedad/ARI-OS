[project]
name = "ari-os"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "sqlite-vec>=0.1.6",
    "scikit-learn>=1.3",
    "httpx>=0.27",
    "pyyaml>=6.0",
    "click>=8.1",
    "mcp>=1.0",
    "requests>=2.31",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
