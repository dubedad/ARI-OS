---
description: Save a durable memory to the local brain.
---
Use the remember skill to capture this into the brain: $ARGUMENTS

The heavy engine stores memories as chunk rows — write the memory file under `$ARI_OS_HOME/memories/<context>/` and ingest via `python3 -m ari_os.tools.cortex ingest --path <file>`.
