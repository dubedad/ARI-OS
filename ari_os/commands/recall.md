---
description: Pull relevant past context from the local brain before answering.
---
Use the recall skill to pull context for: $ARGUMENTS

If a mode override is needed (focus / wide / synthesis / deep / creative / visual), append `--mode <name>` to the `retrieve` call in the skill.
